package chapter01;

import javax.swing.JOptionPane;

public class A_notebook {

	public static void main(String[] args) {
		
		// +더하기 -빼기 *곱하기 /나누기 %나머지 ++ 1증가(전위 후위) -- 1감소(전위 후위)
		// byte short int long 정수 float double 실수
		// 정수->실수 변환 생략가능 실수->정수 변환 생략불가
		// = 대입 +(-*/%)= 결과를 왼쪽에 대입
		// == 왼쪽과 오른쪽이 같은가? != 왼쪽과 오른쪽이 다른가?
		// && 왼쪽과 오른쪽이 모두 참인가, || 하나라도 참인가, ! 참 또는 거짓의 반대
		// 조건 ? 참일때 : 거짓일 때 (?왼쪽의 조건이 참이면 왼쪽 거짓이면 오른쪽)
		/* String s="I like Java"
		 * length      길이     s.length(); 11				/ startsWith  문자열로 시작하는가 s.startsWith("I like");  true
		 * toUpperCase 대문자로  s.toUpperCase(); I LIKE JAVA	/ endsWith    문자열로 끝나는가   s.endsWith(".");        false
		 * toLowerCase 소문자로  s.toLowerCase(); i like java	/ replace     문자열 변환       s.replace("like","love"); I love Java
		 * contains    포함여부  s.contains("Java"); True		/ substring   문자열 자르기      s.substring(7);         Java
		 * indexOf     위치정보  s.indexOf("Java");  7			/ trim        앞뒤 공백 제거     s.trim();               I like Java
		 * lastIndexOf 마지막위치 s.lastindexOf("a"); 10		/ concat      문자열 결합       s.concat(" and Python"); I like Java and Python
		 */
		//int ~~=Integer.parseInt(JOptionPane.showInputDialog());
		//String name=scan.nextLine(); 스캔-한줄전체 입력
		//  \n 줄나누기
		// index -> Hello H=0 e=1 l=2 l=3 o=4 나열느낌
		// JOptionPane.showMessageDialog(null//자리지정, cooking//String,or int같은거 +"이 준비 되었습니다");
		
	}
}
